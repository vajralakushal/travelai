version: '3.8'

services:
  wanderai:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./src:/app/src
      - ./wanderai.db:/app/wanderai.db
    env_file:
      - .env
    restart: unless-stopped