version: '3.8'

services:
  wanderai:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./src:/app/src
      - ./travelai.db:/app/travelai.db
    env_file:
      - .env
    restart: unless-stopped